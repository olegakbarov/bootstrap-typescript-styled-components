{
  "name": "example-browserify",
  "version": "1.0.0",
  "description": "Browserify example for comparison.",
  "author": "Sergey Slipchenko",
  "license": "MIT",
  "scripts": {
    "start": "budo ../src --serve bundle.js --dir ../public -- --debug -t babelify -t [ cssify --modules ] -p browserify-hmr"
  },
  "devDependencies": {
    "babelify": "^6.4.0",
    "browserify": "^12.0.1",
    "browserify-hmr": "^0.3.1",
    "budo": "^7.0.2",
    "cssify": "^1.0.1"
  }
}
